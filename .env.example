# Optional Bearer token for API authentication (leave empty to disable auth)
WA_API_TOKEN=
# Server port (default: 3100)
PORT=3100
# Puppeteer cache directory
PUPPETEER_CACHE_DIR=/home/andrepaim/.cache/puppeteer
